---
title: configure
kind: function
longname: module:parity.configure
description: Options object passed to {@link module:parity.configure|configure}. Inline object type — exercises structured-type expansion.
group: Functions
---

# configure

<Signature
  code="configure(
	opts: { label?: string; point: Point; retries: number },
): void"
/>

<SourceLink href="/source/docs-site/typedoc-src/parity-ts/#L81" label="parity.ts:81" />

Options object passed to [configure](/module/parity/configure). Inline object type — exercises structured-type expansion.

**Parameters**

- `opts` ({ label?: string; point: [Point](/module/parity/point); retries: number })

**Properties**

- `label` (string, optional)
- `point` ([Point](/module/parity/point))
- `retries` (number)

**Returns**

- `void`
