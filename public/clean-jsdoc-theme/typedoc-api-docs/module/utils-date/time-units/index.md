---
title: TIME_UNITS
kind: variable
longname: module:utils/date.TIME_UNITS
description: Time units in milliseconds
---

# TIME\_UNITS

<Signature
  code="TIME_UNITS: {
	DAY: number;
	HOUR: number;
	MINUTE: number;
	MONTH: number;
	SECOND: 1000;
	WEEK: number;
	YEAR: number;
}"
/>

<SourceLink href="/source/docs-site/typedoc-src/utils/date-ts/#L34" label="date.ts:34" />

Time units in milliseconds

**Properties**

- `DAY` (number) — Milliseconds in a day
- `HOUR` (number) — Milliseconds in an hour
- `MINUTE` (number) — Milliseconds in a minute
- `MONTH` (number) — Average milliseconds in a month (30 days)
- `SECOND` (1000) — Milliseconds in a second
- `WEEK` (number) — Milliseconds in a week
- `YEAR` (number) — Average milliseconds in a year (365 days)
